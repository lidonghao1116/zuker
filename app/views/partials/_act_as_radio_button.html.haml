= f.error x
= f.label x
.form-group
  = f.collection_radio_buttons x, HouseData.send("#{x}_categories"), :last, :first do |b|
    = b.label(class: "btn btn-default btn-lg act_as_radio_button #{'btn-warning' if b.value == @house.send(x) }") { b.radio_button(class: "hidden", data: { cancelable: "#{b.value == @house.send(x)? true : false }" } ) + render_option_category(b.value, x) }