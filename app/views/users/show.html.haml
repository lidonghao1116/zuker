%p
  %b= t "user.name"
  = current_user.name
%p
  %b= t "user.phone_number"
  = current_user.phone_number
%p
  %b= t "user.email"
  = current_user.email
%p
  %b= t "user.expected_graduation_date"
  = current_user.expected_graduation_date
%p
  %b= t "user.pin"
  = current_user.pin.content
%p
  %b= t "user.verified"
  - if current_user.verified
    您已完成手機驗證
  - else
    您尚未完成手機驗證
    = link_to I18n.t('Verify'), phone_verify_user_path(current_user)

= link_to I18n.t('Edit'), edit_user_path(current_user)
= link_to I18n.t('Delete'), user_path(current_user), method: :delete, data: {confirm: "Are you sure?"}
