%ul.nav.nav-sidebar
  - ['basic', 'amenity', 'description', 'photo', 'date_status'].each do |s|
    %li{ class: ('active' if action_name == s) }= link_to s, send("#{s}_user_house_path", @user, @house)